apiVersion: scheduling.k8s.io/v1
description: PriorityClass created for understand how it works
kind: PriorityClass
metadata:
  name: priority-class-lab
value: 1000
globalDefault: true # or false, it means that if one Pod was created without definition of PriorityClass this is the default.
